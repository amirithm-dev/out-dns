export type SelectedDNS = {
    name: string;
    primary: string;
    secondary: string;
}

export type DNSEntry = {
    id: string;
    name: string;
    primary_dns: string;
    secondary_dns: string;
}